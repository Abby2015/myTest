<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yum.kfc.brand.bigcake1806.dao.BcUser1806Mapper">
	<select id="getUserInfo" resultType="com.yum.kfc.brand.bigcake1806.pojo.BcUser1806">
		select top 1 * from [${SOCIALDB}]..[bigCake1806] with(nolock)
			where userCode = #{userCode} order by consumeTime desc
	</select>
	
	<select id="getTasteStoreName" resultType="java.lang.String">
		select storeName from [${SOCIALDB}]..[bigCake1806Taste] with(nolock)
			where cityName=#{cityName}
	</select>
</mapper>