<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yum.kfc.brand.dbwy1506.dao.Dbwy1506CouponMapper">
	<insert id="insert" parameterType="com.yum.kfc.brand.dbwy1506.pojo.Dbwy1506Coupon">
		INSERT INTO dbwy1506_coupon
			([id],[openId],[userId],[channelType],[deviceType],[inviterId],[inviteChannelType],[partner],[askType],[promoCode],[isPutCardBag],[couponTime])
		VALUES
			(#{id},#{openId},#{userId},#{channelType},#{deviceType},#{inviterId},#{inviteChannelType},#{partner},#{askType},#{promoCode},#{isPutCardBag},#{couponTime})
	</insert>
	
	<select id="getValidCount" resultType="int">
		select count(*) from dbwy1506_coupon with(nolock) where askType=#{askType} and inviterId=#{userId} and userId != #{userId}
		and inviteChannelType=#{channelType} and couponTime between #{fromTime} and #{toTime}
	</select>
	
	<select id="getLastCoupon" resultType="String">
		select top 1 promoCode from dbwy1506_coupon with(nolock) where userId=#{userId} and channelType=#{channelType} order by couponTime desc
	</select>
	
</mapper>