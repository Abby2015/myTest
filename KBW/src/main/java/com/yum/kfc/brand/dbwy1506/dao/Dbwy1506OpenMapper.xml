<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yum.kfc.brand.dbwy1506.dao.Dbwy1506OpenMapper">
	<insert id="insert" parameterType="com.yum.kfc.brand.dbwy1506.pojo.Dbwy1506Open">
		INSERT INTO dbwy1506_open
			([id],[userId],[inviterId],[channelType],[deviceId],[deviceType],[token],[inviteChannelType],[tagContent],[ipAddr],[openTime])
		VALUES
			(#{id},#{userId},#{inviterId},#{channelType},#{deviceId},#{deviceType},#{token},#{inviteChannelType},#{tagContent},#{ipAddr},#{openTime})
	</insert>
	
	<select id="getTagContent" parameterType="String" resultType="String">
		select tagContent from dbwy1506_open with(nolock) where id=#{openId}
    </select>
</mapper>