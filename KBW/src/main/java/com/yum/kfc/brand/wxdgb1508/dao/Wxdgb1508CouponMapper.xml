<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yum.kfc.brand.wxdgb1508.dao.Wxdgb1508CouponMapper">
	<insert id="insert" parameterType="com.yum.kfc.brand.wxdgb1508.pojo.Wxdgb1508Coupon">
		INSERT INTO wxdgb1508_coupon
			([id],[openId],[userId],[channelType],[deviceType],[inviterId],[inviteChannelType],[isPutCardBag],[promoCode],[couponTime])
		VALUES
			(#{id},#{openId},#{userId},#{channelType},#{deviceType},#{inviterId},#{inviteChannelType},#{isPutCardBag},#{promoCode},#{couponTime})
	</insert>
	
	<select id="getTodayCoupon" resultType="String">
		select top 1 promoCode from wxdgb1508_coupon with(nolock) where userId=#{userId} 
		and channelType=#{channelType} and couponTime between #{fromTime} and #{toTime}
	</select>
	
	<select id="isDrawCoupon" resultType="com.yum.kfc.brand.wxdgb1508.pojo.Wxdgb1508Coupon">
		select * from wxdgb1508_coupon with(nolock) where inviterId=#{userId}
		and inviteChannelType=#{channelType}
	</select>
	
	<select id="getLastCoupon" resultType="String">
		select top 1 promoCode from wxdgb1508_coupon with(nolock) where userId=#{userId} and channelType=#{channelType}
	</select>
</mapper>